import style from './Header.module.css';

export const Header = () => {

    return (
        <header className={style.header}>
            <h1>Цифровая среда для проведения выборов и голосования</h1>
        </header>
    );
};
